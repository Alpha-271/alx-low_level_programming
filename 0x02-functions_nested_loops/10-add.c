#include "main.h"
/**
 * add - a function that adds two numbers
 * @i: stores first input
 * @x: stores second input
 * Return: returns the result
 */

int add(int i, int x)
{
	int n;

	n = i + x;
	return (n);
}
